---
export interface Props {
  url: string;
  slot: string;
}

const {slot, url} = Astro.props;
---

<astro-slot-observer data-url={url} slot={slot} set:html={`<!--#include virtual="${url}" -->`}></astro-slot-observer>

<script>
  import AstroSlotObserver from './AstroSlotObserver.ts';
</script>
